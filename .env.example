# ─── Root .env.example — copy per service ─────────────────────────────────
# See backend/api-service/.env.example for full API service config
# See backend/video-processor/.env.example for full job config

# ─── Shared GCP Settings ──────────────────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-east1

# ─── Buckets ──────────────────────────────────────────────────────────────
GCS_UPLOADS_BUCKET=your-project-id-uploads
GCS_PROCESSED_BUCKET=your-project-id-processed
CDN_BASE_URL=https://cdn.your-domain.com

# ─── Database ─────────────────────────────────────────────────────────────
DB_HOST=10.x.x.x
DB_PORT=5432
DB_NAME=creator_mvp
DB_USER=clipora
DB_PASSWORD=change-me

# ─── Auth ─────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
MVP_API_KEY=change-me-generate-random-32-hex-string

# ─── Mobile App ───────────────────────────────────────────────────────────
EXPO_PUBLIC_API_URL=https://your-cloud-run-url.run.app
EXPO_PUBLIC_API_KEY=change-me-same-as-mvp-api-key
